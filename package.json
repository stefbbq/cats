{
  "scripts": {
    "dev": "wrangler dev src/index.tsx",
    "deploy": "wrangler publish src/index.ts",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-01-01 ./assets",
    "pages:deploy": "wrangler pages deploy ./pages"
  },
  "dependencies": {
    "hono": "^3.7.2",
    "npm": "^10.2.0"
  },
  "devDependencies": {
    "bun-types": "^0.6.2",
    "@cloudflare/workers-types": "^4.20230914.0",
    "wrangler": "^3.11.0"
  }
}
